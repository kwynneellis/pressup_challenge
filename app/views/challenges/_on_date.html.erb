<div class="bubble-container" id="challenge-info">

  <div class="row text-center">
    <% if reps_remaining > 0 %>
    <div class="col"><%= reps_done %>/<%= rep_target %></div>
    <div class="col"><h4><%= reps_remaining %> left</h4></div>
    <% else %>
    <div class="col"><h4>All <%= reps_done %> done 👏</h4></div>
    <% end %>

  </div>

  <%= render 'challenges/progress_bar', reps_done_as_percentage: reps_done_as_percentage %>
</div>
